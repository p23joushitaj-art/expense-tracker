// ---------------------------------------------------------------------------
// Firebase configuration
// ---------------------------------------------------------------------------
// Replace the placeholder values below with YOUR Firebase project's config.
// You get these from the Firebase Console:
//   Project settings (gear icon) -> "Your apps" -> Web app -> "SDK setup and
//   configuration" -> "Config".
//
// It is SAFE to commit these values to a public GitHub repo. They are not
// secrets — your data is protected by Firestore Security Rules + Anonymous
// Auth, not by hiding this config. (See README.md.)
// ---------------------------------------------------------------------------

export const firebaseConfig = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT.firebaseapp.com",
  projectId: "PASTE_YOUR_PROJECT_ID",
  storageBucket: "PASTE_YOUR_PROJECT.appspot.com",
  messagingSenderId: "PASTE_YOUR_SENDER_ID",
  appId: "PASTE_YOUR_APP_ID"
};
